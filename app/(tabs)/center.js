// app/(tabs)/center.js
// ISSY Resident App - Center Tab (Logo placeholder)
// This screen is not directly accessed - the logo redirects to home

import { View } from 'react-native';

export default function Center() {
  // This screen exists only to satisfy the tab structure
  // The center button redirects to home
  return <View />;
}